version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 -c "import app; print('Import successful')" && uvicorn app:app --host 0.0.0.0 --port 8080 --log-level debug
  network:
    port: 8080
