version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install --no-cache-dir -r requirements.txt
run:
  command: python -c "print('Python works')" && uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
